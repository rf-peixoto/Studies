import secrets

dir(secrets)
dir(secrets.token_bytes)

#import sys

#libname = sys.argv[1]
#try:
#    exec('import {0}'.format(libname))
#    exec('dir({0})'.format(libname))
#except Exception as error:
#    print(error)
