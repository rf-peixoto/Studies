services:
  qdrant:
    image: qdrant/qdrant:latest
    restart: unless-stopped
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC (unused by this app, available for future use)
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
