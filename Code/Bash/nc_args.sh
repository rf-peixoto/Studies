# --send-only to ignore received data. Use as protection
# on reverse connections. Imagine that you have nc ready
# to send code to be executed on a host. Use this.
nc --send-only

# ms: milliseconds;
--delay 500ms 30s 5m 1h
